# termseries configuration
# Copy to .termseries.env in your project directory or
# ~/.config/termseries/termseries.env for user-global settings.

# Plot theme: dark, light, or auto (default — follows terminal background)
# THEME=auto
